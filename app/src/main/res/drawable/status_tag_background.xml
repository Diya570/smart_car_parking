<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <corners android:radius="@dimen/status_tag_corner_radius" />
    <!-- The actual color will be set via android:backgroundTint in the layout -->
    <solid android:color="@color/status_green_bg" />
</shape>